export default function Logo({ className = '' }) {
  return (
    <span className={`font-display text-xl tracking-tight text-ink ${className}`}>
      Care<span className="text-teal-500">Confide</span>
    </span>
  )
}
